package com.storyreader.data.db.converter

import androidx.room.TypeConverter

class Converters {
    // Room handles String and Long natively.
    // Add converters here when custom types are needed.
}
